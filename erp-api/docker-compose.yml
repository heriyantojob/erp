services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    stdin_open: true
    tty: true # Keeps the container running for debugging
    env_file:
      - .env
    command: npm run start
